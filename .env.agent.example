# Copy to .env.agent (gitignored). One per machine/session.
# Default below is right for the /work orchestrator folder; workers (coder/reviewer
# subagents) overwrite this from their spawn prompt, so leave it as-is elsewhere too.
AGENT_NAME="FAB MacBook Orchestrator"   # machine first: MacBook | MacMini | Cloud
